# =============================================================================
# .env.example — copy to .env and fill in your values
# =============================================================================
# docker compose reads .env automatically from the project root.

# Port the production container (or backend-dev) binds on the host.
PORT=8000

# Ports used in dev profile only.
BACKEND_PORT=8000
FRONTEND_PORT=5173
